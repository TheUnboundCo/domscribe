{
  "name": "domscribe-overlay",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "packages/domscribe-overlay/src",
  "tags": ["scope:infra", "type:lib", "type:test"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{workspaceRoot}/dist/packages/domscribe-overlay"],
      "options": {
        "rootDir": "packages/domscribe-overlay/src",
        "outputPath": "dist/packages/domscribe-overlay",
        "main": "packages/domscribe-overlay/src/index.ts",
        "tsConfig": "packages/domscribe-overlay/tsconfig.lib.json",
        "generatePackageJson": true,
        "generateExportsField": true,
        "assets": ["packages/domscribe-overlay/*.md"]
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "eslintConfig": "eslint.config.mjs",
        "lintFilePatterns": [
          "packages/domscribe-overlay/**/*.ts",
          "packages/domscribe-overlay/**/*.tsx"
        ]
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{projectRoot}/test-output"],
      "options": {
        "config": "packages/domscribe-overlay/vite.config.ts"
      }
    },
    "typecheck": {
      "executor": "nx:noop"
    },
    "watch-deps": {
      "executor": "nx:noop"
    },
    "build-deps": {
      "executor": "nx:noop"
    }
  }
}
