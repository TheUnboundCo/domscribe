{
  "name": "@domscribe/fixture-<%= bundler %>-v<%= bundlerVersion %>-<%= framework %>-<%= frameworkVersion %>-<%= language %>",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:production": "vite build --mode production",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^<%= frameworkVersion %>.0.0",
    "react-dom": "^<%= frameworkVersion %>.0.0"
  },
  "devDependencies": {
    "@domscribe/react": "*",
    "@types/react": "^<%= frameworkVersion %>.0.0",
    "@types/react-dom": "^<%= frameworkVersion %>.0.0",
    "@vitejs/plugin-react": "^4.7.0",
    "vite": "^<%= bundlerVersion %>.0.0"<% if (language === 'ts') { %>,
    "typescript": "^5.5.0"<% } %>
  }
}
