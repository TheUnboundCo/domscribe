/**
 * @module @domscribe/relay/program
 *
 * Public entry point for the CLI program instance.
 * Used by wrapper packages (domscribe, @domscribe/mcp) to invoke the CLI.
 */
export { program } from '../cli/program.js';
