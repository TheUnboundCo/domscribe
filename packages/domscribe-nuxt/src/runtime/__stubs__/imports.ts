/**
 * Test stub for Nuxt's #imports virtual module.
 * vi.mock('#imports', ...) replaces this at runtime — this file only
 * exists so Vite's import analysis can resolve the bare specifier.
 */
export function defineNuxtPlugin(plugin: () => void | Promise<void>): unknown {
  return plugin;
}
